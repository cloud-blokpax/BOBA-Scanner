// Enable SSR for the DBS Calculator page so it renders on the server.
// This allows search engines to index the page and social media platforms
// to generate Open Graph previews when the URL is shared.
export const ssr = true;
