/**
 * Game 3 — visual theme (skeleton).
 *
 * Neutral slate palette until the real game has branding. Replace with
 * final brand colors when Game 3 is announced.
 */

import type { GameTheme } from '../types';

export const game3Theme: GameTheme = {
	accentPrimary: '#64748b',   // slate-500 — neutral
	accentSecondary: '#94a3b8', // slate-400
	cardBg: '#0f172a',          // slate-900
	textAccent: '#cbd5e1',      // slate-300
};
