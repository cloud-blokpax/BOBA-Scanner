/**
 * Game 3 — card number extraction (skeleton).
 *
 * Returns null until the real Game 3 is announced and implemented.
 * When implementing, mirror the pattern from boba/extract.ts or
 * wonders/extract.ts depending on which fits Game 3's numbering scheme.
 */

/** Known Game 3 card number prefixes. Empty until the real game is defined. */
export const GAME3_PREFIXES = new Set<string>([]);

/**
 * Extract a Game 3 card number from raw OCR text.
 * Always returns null — the real implementation goes here.
 */
export function extractGame3CardNumber(_text: string): string | null {
	return null;
}
