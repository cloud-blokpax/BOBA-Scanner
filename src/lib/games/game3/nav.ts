/**
 * Game 3 — navigation items + protected routes (skeleton).
 *
 * Empty until the real Game 3 has routes. Mirror boba/nav.ts or
 * wonders/nav.ts when implementing.
 */

import type { GameNavItem } from '../types';

export const game3NavItems: readonly GameNavItem[] = [];

export const game3ProtectedRoutes: readonly string[] = [];
