/**
 * BoBA visual theme — gold accent, dark vault aesthetic.
 *
 * Matches the existing design system in src/styles/index.css.
 */

import type { GameTheme } from '../types';

export const bobaTheme: GameTheme = {
	accentPrimary: '#f59e0b',   // Gold (--gold)
	accentSecondary: '#d97706', // Dark gold (--gold-dark)
	cardBg: '#0d1524',          // Surface (--bg-surface)
	textAccent: '#f59e0b',      // Gold accent text
};
